# Docker Template for PHP development 
### commands
##### docker-compose file
```
docker-compose up -d
```
```
docker-compose down
```
##### Dockerfile
```
 docker run -it  --rm --name my-running-app my-php-app
```
